event: account.updated
steps:
  - method: POST
    path: /api/v1/accounts
    body:
      name: Test Account
    capture: account_id
  - method: PATCH
    path: /api/v1/accounts/{account_id}
    body:
      name: Updated Account Name
