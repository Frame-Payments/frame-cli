event: account.updated
steps:
  - method: POST
    path: /accounts
    body:
      name: Test Account
    capture: account_id
  - method: PATCH
    path: /accounts/{account_id}
    body:
      name: Updated Account Name
