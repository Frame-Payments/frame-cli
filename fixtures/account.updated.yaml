event: account.updated
steps:
  - method: POST
    path: /accounts
    body:
      type: individual
      profile:
        individual:
          email: test@framepayments.dev
    capture: account_id
  - method: PATCH
    path: /accounts/{account_id}
    body:
      name: Updated Account Name
