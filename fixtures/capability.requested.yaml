event: capability.requested
steps:
  - method: POST
    path: /accounts
    body:
      name: Test Account
    capture: account_id
  - method: POST
    path: /capabilities
    body:
      account_id: "{account_id}"
      type: card_receive
    capture: capability_id
